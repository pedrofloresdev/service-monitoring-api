# /project:create-plan

Use the `planner` agent.

Requires: an approved `specs/{feature-name}/spec.md` must exist.

Read the Memory Bank first (see CLAUDE.md Step 0).
Read the approved spec completely before planning.

Generate a plan file at `specs/{feature-name}/plan.md` that describes the
architecture and technical approach to implement the spec.

After creating the file, display its full contents and wait for flobell to
write "approved" before proceeding.
